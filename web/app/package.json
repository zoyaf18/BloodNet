{
  "name": "bloodnet-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "deploy:hosting": "npm run build && firebase deploy --only hosting --project bloodnet-app --non-interactive",
    "preview": "vite preview",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "firebase": "^12.18.0",
    "leaflet": "^1.9.4",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@playwright/test": "latest",
    "@types/leaflet": "^1.9.22",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@vitejs/plugin-react": "latest",
    "typescript": "latest",
    "vite": "latest"
  }
}
